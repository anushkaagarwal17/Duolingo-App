services:
  - type: web
    name: duolingo-english-app
    runtime: python
    plan: free
    buildCommand: bash build.sh
    startCommand: cd backend && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
