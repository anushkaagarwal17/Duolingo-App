services:
  - type: web
    name: duolingo-backend
    env: python
    plan: free
    rootDir: duolingo-app/backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
    branch: master